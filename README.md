## Abrakadabra Gems
- Handy LLM Gems with preloaded domain context
- self-contained and standalone
- `AGENT.md` file is the init prompt e.g. for Gemini Gem
- The rest of the MD files in each directory are just optional knowledge, you could load your own